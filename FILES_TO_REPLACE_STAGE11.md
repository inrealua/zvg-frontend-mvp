# Stage 11 — файлы для замены/добавления

Скопировать в корень проекта `D:\Projects\zvg_frontend_mvp`:

```txt
app/globals.css
app/properties/[id]/page.tsx
components/PropertyGallery.tsx
components/PropertyDetailActions.tsx
```

База данных не меняется.
`npm run db:push` не нужен.
`npm run db:seed` не запускать.
