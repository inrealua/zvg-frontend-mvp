#!/usr/bin/env node
/* eslint-disable no-console */
require('./load_env.cjs');const{PrismaClient}=require('@prisma/client');const prisma=new PrismaClient();
async function main(){const cols=await prisma.$queryRawUnsafe('SHOW COLUMNS FROM `Property`');const names=new Set(cols.map(x=>x.Field));const required=['canonicalId','publicationStatus','analysisVersion','primarySourcePortal','investmentScore','bidMaximumEur','marketJson','enrichmentJson','qualityGateJson','constructionRisksJson','legalRisksJson'];const tcols=await prisma.$queryRawUnsafe('SHOW COLUMNS FROM `PropertyTranslation`');const tn=new Set(tcols.map(x=>x.Field));const tr=['shortDescription','longDescription','buildingAndLayout','localizedFieldsJson','marketSummary','riskSummary'];console.log('DB schema:',required.every(x=>names.has(x))&&tr.every(x=>tn.has(x))?'READY':'NOT READY');for(const x of required)console.log(names.has(x)?'[ok]':'[missing]',`Property.${x}`);for(const x of tr)console.log(tn.has(x)?'[ok]':'[missing]',`PropertyTranslation.${x}`);if(required.every(x=>names.has(x))){const counts=await prisma.property.groupBy({by:['publicationStatus'],_count:{_all:true}}).catch(()=>[]);console.log('Publication status counts:',counts);}console.log('Users:',await prisma.user.count(),'Saved searches:',await prisma.savedSearch.count());}
main().catch(e=>{console.error(e);process.exitCode=1}).finally(()=>prisma.$disconnect());
